{
    "name": "hdfs-sink",
    "config": {
        "connector.class": "io.confluent.connect.hdfs.HdfsSinkConnector",
        "tasks.max": 1,
        "topics": "test_hdfs",
        "hdfs.url": "hdfs://namenode:8020",
        "flush.size": 3
    }
}